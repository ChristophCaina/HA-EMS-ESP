{
  "config": {
    "step": {
      "user": {
        "title": "Set up EMS-ESP Gateway",
        "description": "Connects to an EMS-ESP gateway using the already configured MQTT integration.",
        "data": {
          "base_topic": "MQTT Base Topic (e.g. ems-esp)",
          "device_name": "Device name (optional, auto-detected from gateway)"
        },
        "data_description": {
          "base_topic": "The MQTT base topic configured in your EMS-ESP gateway (default: ems-esp).",
          "device_name": "Leave empty to auto-detect from EMS-ESP product ID."
        }
      }
    },
    "abort": {
      "already_configured": "This base topic is already configured.",
      "mqtt_not_configured": "The MQTT integration is not set up. Please configure MQTT first."
    },
    "error": {}
  },
  "options": {
    "step": {
      "init": {
        "title": "EMS-ESP Options",
        "data": {
          "base_topic": "MQTT Base Topic (e.g. ems-esp)",
          "device_name": "Device name (optional)"
        }
      }
    }
  }
}